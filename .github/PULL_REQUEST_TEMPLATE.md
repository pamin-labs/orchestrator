<!--
  Two checks will run and both have to pass:

    check  — tsc, oxlint, build:web, bun test           (bun test locally first)
    dco    — every commit signed off                     (git commit -s)

  CONTRIBUTING.md has the details, CLAUDE.md has the house rules.
-->

## What this fixes

<!-- The failure, not the diff: what it cost, how it presented. If it is a new
     feature, what was awkward without it. -->

## How you know

<!-- The check that fails without this change, or the measurement. Numbers beat
     adjectives — `179 on the host, 0 in the container` decides something. -->
